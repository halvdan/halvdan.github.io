<!DOCTYPE HTML>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>halvdan</title>
    </head>
    <body>
        {{> header }}

        <ul>
            {{#each posts }}
            <li>
                <span>{{ formatDate date }}</span>
                <a href="{{path}}">{{ title }}</a>
            </li>
            {{/each }}
        </ul>

        {{> footer }}
    </body>
</html>
