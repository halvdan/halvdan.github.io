<!DOCTYPE HTML>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>halvdan</title>
    </head>
    <body>
        {{> header }}

        <ul>
            {{#each posts }}
            <span>{{ formatDate date }}</span>
            <a href="{{path}}">{{ title }}</a>
            {{/each }}
        </ul>

        {{> footer }}
    </body>
</html>
