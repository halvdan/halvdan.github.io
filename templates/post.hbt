<!DOCTYPE HTML>
<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width" />
        <title>halvdan | {{ title }}</title>
    </head>
    <body>
        {{> header }}
        <h1>{{ title }}</h1>

        <div class="timestamp">
            <p>Posted at: <span class="date">{{ formatDate date }}</span></p>
            {{#if updatedAt }}
                <p>Last modified at: <span class="date">{{ formatDate updatedAt }}</span></p>
            {{/if }}
        </div>

        <article>
            {{{ contents }}}
        </article>

        {{> footer }}
    </body>
</html>
